<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:checkableBehavior="single">

        <item android:id="@+id/newsDashboardFragment"
            android:title="Home"
            android:checked="true"
            app:iconTint="@drawable/bottom_nav_selector"
            android:icon="@drawable/ic_home_selected" />

        <item android:id="@+id/savedNewsFragment"
            android:title="Favourite"
            app:iconTint="@drawable/bottom_nav_selector"
            android:icon="@drawable/ic_favorite_selected" />

        <item android:id="@+id/profileFragment"
            android:title="Profile"
            app:iconTint="@drawable/bottom_nav_selector"
            android:icon="@drawable/ic_profile_selected" />

    </group>
</menu>